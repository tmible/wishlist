/**
 * Перечисление разделов справки
 * @enum {string}
 */
const HelpSections = Object.freeze({
  GENERAL: 'general',
  FOREIGN_LIST: 'foreign-list',
  NICKNAME: 'nickname',
  COMMAND_LIST: 'command-list',
});

export default HelpSections;
